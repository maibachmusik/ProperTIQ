"""ProperTIQ standalone config app — a no-code strategy builder.

A separate, optional deployable (install with ``propertiq[app]``). The core
``propertiq`` library does not depend on this package. See ``spec 003``.
"""
